# Direct dependencies, exact-pinned for reproducible installs.
# This file is the deploy manifest (Streamlit Cloud) and the source of the
# package dependencies in pyproject.toml. Pins mirror the blessed local
# environment (Python 3.11); bump deliberately, one at a time.

# LLM / RAG stack
langchain==1.3.1
langchain-chroma==1.1.0
langchain-classic==1.0.7
langchain-core==1.4.0
langchain-community==0.4.2
langchain-google-genai==4.2.3
chromadb==1.5.9
sentence-transformers==5.5.1
# CI installs torch from the CPU wheel index first (see deploy.yml); the
# +cpu local build satisfies this pin, so it is not reinstalled from PyPI.
torch==2.12.0
langfuse==4.6.1

# Web app
# The streamlit pin must track the version Streamlit Cloud runs. Pinning below
# 1.58 broke sign-in in production: st.context.cookies came back empty under
# Cloud's iframe-embedded serving, so browser-cookie session restore failed.
streamlit==1.59.2

# Database
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12

# Auth / email
PyJWT==2.13.0
email-validator==2.3.0
sendgrid==6.12.5

# Ingestion / utilities
PyMuPDF==1.27.2.3
pandas==3.0.3
requests==2.34.2
tenacity==9.1.4
tqdm==4.67.3
python-dotenv==1.2.2
PyYAML==6.0.3
